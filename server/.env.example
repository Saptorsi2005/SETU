# ============================================
# SETU Server Environment Variables
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# ============================================
# Database Configuration (Neon PostgreSQL)
# ============================================
DB_HOST=your-neon-hostname.neon.tech
DB_PORT=5432
DB_NAME=setu_db
DB_USER=your-neon-username
DB_PASSWORD=your-neon-password
DB_SSL=true
DB_SSL_MODE=require

# ============================================
# JWT Configuration
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# ============================================
# Cloudinary Configuration (Image Uploads)
# ============================================
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# ============================================
# Email Configuration (OTP for Student Signup)
# ============================================
# For Gmail (Recommended for development):
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password below (NOT your regular Gmail password)

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

# For other SMTP providers (SendGrid, Mailgun, etc.):
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASS=your-sendgrid-api-key

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# 1. DATABASE (Neon)
#    - Sign up at https://neon.tech
#    - Create a new project
#    - Copy connection details to DB_* variables above
#    - Set DB_SSL=true for Neon

# 2. CLOUDINARY (Image Storage)
#    - Sign up at https://cloudinary.com (free tier available)
#    - Go to Dashboard
#    - Copy Cloud Name, API Key, API Secret
#    - Paste into CLOUDINARY_* variables above

# 3. EMAIL SERVICE (for OTP)
#    - For Gmail: Enable 2FA and create App Password
#    - Go to: https://myaccount.google.com/apppasswords
#    - Generate 16-character password
#    - Use it in EMAIL_PASS (not your regular Gmail password)
#    - Alternatively, use SendGrid/Mailgun for production

# 4. JWT_SECRET
#    - Generate a strong random string
#    - Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# 5. COPY THIS FILE
#    - Copy this file to .env
#    - Replace all placeholder values
#    - NEVER commit .env to Git (already in .gitignore)

# ============================================
# PRODUCTION DEPLOYMENT
# ============================================
# For Vercel/Render/Railway:
# - Add all these environment variables in the platform's dashboard
# - Make sure to set NODE_ENV=production
# - Cloudinary URLs work automatically (no filesystem dependency)
